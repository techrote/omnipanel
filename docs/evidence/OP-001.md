# OP-001 implementation evidence

Status: implementation and automated acceptance PASS; independent Stone review and
merge PENDING. Issue #10 and parent OP-M001 / #1 remain open. PR: #56.
Branch: `implement/op-001-python-foundation`.
Base commit: `a27d37a2bf11508da45caa8d217fb504aa61f119`.
Base tree: `ae712b9551e9c8c24917e06c973a95afef6ef5f7`.
Tested implementation head: `977c968731721a86411c60fca51c42ec2c833fc5`.
The following evidence-only commit does not change source, tests, dependencies or CI.

## Scope and inputs

Read README, AGENT_CONTEXT, PROGRAMME_HANDOVER, SYSTEM_ARCHITECTURE, TRUST_MODEL,
VERIFY, METAISSUES, DECISIONS, ORCHESTRATION_MODEL, ROADMAP, BLOCKERS, workflow.json,
issue-bindings.json, EXECUTION_LEDGER and issues #1/#10 before implementation.
The base was documentation-only, with no existing PR or alternate branch.
OP-001 had no prerequisites. No other repository was changed.

Added conventional src packaging, immutable typed startup configuration, inert CLI/status,
lazy Textual startup, synthetic fixtures, regression/boundary/UI tests and CI.
No task schema, scheduler, persistence, provider, secret loading or privileged host
operation has been introduced. OP-007 still owns the actual operator application.
The original planning corpus/DAG/bindings are preserved, except for README's scoped
implementation-status and startup additions. Developer/evidence docs do not replace
canonical specifications. Dependency rationale is in DEVELOPMENT.md.

## Changed-file manifest

```text
.editorconfig
.gitattributes
.github/workflows/ci.yml
.gitignore
MANIFEST.in
README.md
pyproject.toml
examples/omnipanel.toml
docs/DEVELOPMENT.md
docs/evidence/OP-001.md
docs/evidence/OP-001-ci.json
scripts/check_distribution.py
src/omnipanel/__init__.py
src/omnipanel/__main__.py
src/omnipanel/_version.py
src/omnipanel/cli.py
src/omnipanel/config.py
src/omnipanel/domain/__init__.py
src/omnipanel/py.typed
src/omnipanel/ui/__init__.py
src/omnipanel/ui/app.py
tests/conftest.py
tests/test_boundaries.py
tests/test_cli.py
tests/test_config.py
tests/test_packaging.py
tests/test_ui.py
```

The remote comparison showed only the bounded implementation additions and README
changes. Local build/cache/egg-info files are ignored and were not published. No
untracked source is needed for the CI result; CI builds the committed repository.

## Local verification and limitations

Environment: Linux x86_64, CPython 3.13.5, pytest 9.0.2, pytest-asyncio 1.3.0,
setuptools 82.0.1. Local package-index/GitHub download host resolution failed.
Repository reads/writes used the connected GitHub tool. Local source materialization
was not a complete Git clone.

```text
uv pip install --offline --python /opt/pyvenv/bin/python --no-build-isolation --no-deps -e /mnt/data/omnipanel
python -m pytest --ignore=tests/test_ui.py -q
python -m compileall -q src tests
```

Restricted editable install: PASS for the package only, with dependencies omitted.
Headless local tests: **59 passed, 3 skipped** (native Windows cases on Linux).
Compilation and direct setuptools backend sdist/wheel build: PASS. A separate local
clean wheel smoke install also passed import/version/metadata/console checks without
dependencies. Original unchanged planning docs were not all materialized locally.

Textual, Ruff, mypy, dependency-complete installation and native Windows checks were
NOT RUN locally. The full repository CI below provides those separate results.

## Full repository CI evidence

[Foundation CI run 34725770901](https://github.com/techrote/omnipanel/actions/runs/34725770901)
completed successfully for the tested implementation head. All six jobs and every
acceptance step were read back. All six artifact ZIPs were downloaded, their SHA-256
values matched GitHub's reported digests, and their JUnit XML was parsed.
`OP-001-ci.json` retains job/artifact identities, hashes, exact interpreter versions,
test counts, skip reasons, elapsed test times and installed-package snapshots.
Those snapshots include preinstalled runner tooling; they are not a dependency lockfile.

| Matrix | Actual Python | Passed | Skipped | Job ID | Result |
|---|---|---:|---:|---|---|
| Linux / 3.12 | 3.12.14 | 64 | 3 | 103639499226 | PASS |
| Linux / 3.13 | 3.13.15 | 64 | 3 | 103639499188 | PASS |
| Linux / 3.14 | 3.14.7 | 64 | 3 | 103639499146 | PASS |
| Windows / 3.12 | 3.12.10 | 64 | 3 | 103639499172 | PASS |
| Windows / 3.13 | 3.13.15 | 64 | 3 | 103639499220 | PASS |
| Windows / 3.14 | 3.14.7 | 64 | 3 | 103639499097 | PASS |

Each job collected 67 tests with zero failures/errors. The three skips are exclusively
opposite-platform path cases: native Windows tests run on Windows; foreign-Windows-path
rejection tests run on Linux. There are no skipped UI tests.

Exact acceptance commands, run from the repository root:

```text
python -m pip install -e ".[dev]"
python -m ruff check .
python -m ruff format --check --diff .
python -m mypy
python -m pytest --junitxml=artifacts/pytest.xml
python -m build
python scripts/check_distribution.py dist/omnipanel-*.whl
python -m pip freeze --all
python -VV
```

All passed on all six jobs. The build command produced an sdist and built the wheel
from that sdist. The distribution probe creates a clean environment outside the
checkout, installs the wheel WITH dependencies, runs pip check, validates module and
console entry points/metadata/py.typed, and exercises real Textual mouse-close startup.
This is not a mocked UI or an inference from the restricted local installation.

### Failures found and corrected

Initial run 34725643362 at `8b948f844d037c2bcda9a8888e4565e363facf44` found four
formatting differences and one UI test using a nonexistent public `Static.markup`
attribute. Commit `977c968731721a86411c60fca51c42ec2c833fc5` applied formatter feedback
and changed the assertion to check public `Static.visual` / `Content.plain` / spans.
The test now verifies literal rendered paths and absence of markup styling directly;
no test was removed, skipped or weakened to obtain green CI.

The pinned artifact uploader emitted a non-failing Node compatibility/deprecation
warning on the observed first run. Artifact upload succeeded; no unsafe Node override
was added. CI artifacts expire after 14 days; this checked-in summary retains the
acceptance result and evidence identities after that expiry.

## Acceptance mapping

| Issue criterion | Evidence |
|---|---|
| Clean install/dev install on supported Python | Editable development install plus fresh dependency-complete wheel install PASS across all six jobs |
| Package import and minimal entry point work | CLI/module/metadata tests, real headless Textual and clean wheel startup PASS |
| Unit/lint baseline green | 64 passed / 3 platform-specific skips per job; Ruff lint/format and strict source mypy PASS |
| Core domain needs no Textual imports | Subprocess import blocker and source import-boundary tests PASS |
| README/dev commands explicit and reproducible | README + DEVELOPMENT + CI + distribution probe; commands exercised on Windows/Linux |

## Independent review and next frontier

Automated acceptance and implementer checks are not independent implementation review.
Stone review: **NOT RUN**. Merge: **NOT DONE**. Issues #10 and #1 remain open.
No automatic issue-closing keyword or automatic merge is requested.

Review PR #56 against the recorded base and issue #10. Inspect configuration failures,
Windows/Unicode paths, import side effects, package contents, literal UI rendering,
startup/close behaviour and test omissions. Reproduce the documented checks, record
independent findings and resolve blockers before approving completion. Do not replan
or broaden OP-001 into task schemas or scheduler implementation during this review.

EXECUTION_LEDGER explicitly makes OP-002 / #11 ready only after OP-001 evidence is
merged. OP-002 was therefore not started, and no prerequisite/review policy was
weakened to manufacture a larger implementation count.

No live component integration or manual Windows 11/Windows Terminal session was
qualified. Hosted Windows CI and headless UI are narrower evidence. UI reopen tests
preserve an inert fixture; they do not prove durable job lifetime/recovery. Those
capabilities belong to subsequent issues. Direct versions are pinned; transitive
resolution is recorded rather than fully locked. No integration contract was changed.
