# OP-001 implementation evidence

Status: implementation candidate; validation and independent review are distinct.
Issue: #10. Parent: OP-M001 / #1. Load-bearing: Stone.
Branch: `implement/op-001-python-foundation`.
Base commit: `a27d37a2bf11508da45caa8d217fb504aa61f119`.
Base tree: `ae712b9551e9c8c24917e06c973a95afef6ef5f7`.

## Scope and inputs

Read README, AGENT_CONTEXT, PROGRAMME_HANDOVER, SYSTEM_ARCHITECTURE, TRUST_MODEL,
VERIFY, METAISSUES, DECISIONS, ORCHESTRATION_MODEL, ROADMAP, BLOCKERS, workflow.json,
issue-bindings.json, EXECUTION_LEDGER and issues #1/#10 before implementation.
The base was documentation-only, with no existing PR or alternate branch.
OP-001 had no prerequisites. No other repository was changed.

Added conventional src packaging, minimal typed configuration, inert CLI/status,
lazy Textual startup, synthetic fixtures, regression/boundary/UI tests and CI.
No task schema, scheduler, persistence, provider, secret loading or privileged host
operation has been introduced. OP-007 still owns the actual operator application.
The original planning corpus/DAG/bindings are preserved, except for README's scoped
implementation-status and startup additions. New developer/evidence docs do not
replace canonical specifications.

## Local verification

Environment: Linux x86_64, CPython 3.13.5, pytest 9.0.2, pytest-asyncio 1.3.0,
setuptools 82.0.1. The container could not resolve package-index/GitHub download
hosts, so repository content was read/published through the connected GitHub tool.
Local source files were materialized for execution; this was not a complete Git clone.

Commands already executed:

```text
uv pip install --offline --python /opt/pyvenv/bin/python --no-build-isolation --no-deps -e /mnt/data/omnipanel
python -m pytest --ignore=tests/test_ui.py -q
python -m compileall -q src tests
```

Editable build/install: PASS for the local package only (runtime dependencies omitted).
Headless unit/import/CLI/configuration/metadata tests: **59 passed, 3 skipped**.
The three skips are native Windows path tests on Linux; Windows CI must cover them.
Compilation: PASS. Direct setuptools backend sdist/wheel build: PASS; original
unchanged planning documents were not all materialized for this restricted local build.

Textual tests, lint, formatting, strict mypy, full dependency installation and native
Windows validation: **NOT RUN locally**, pending repository CI. No claim of a fully
validated development installation follows from the `--no-deps` smoke check.

## Reproducible full verification

`docs/DEVELOPMENT.md` gives exact commands. `.github/workflows/ci.yml` runs editable
installation, Ruff lint/format, strict source mypy, all pytest tests, sdist/wheel build
and a fresh wheel install outside the checkout on Windows/Linux and Python
3.12/3.13/3.14. Each matrix job captures JUnit and the resolved dependencies.
The clean-wheel probe executes actual headless Textual, not a mocked dependency.
CI results must be read back and recorded before describing these gates as passed.

## Acceptance mapping

| Issue criterion | Evidence / outstanding gate |
|---|---|
| Clean install/dev install on supported Python | Restricted local editable install passes; full editable + clean wheel pending CI |
| Package import and minimal entry work | Local subprocess/module/status tests pass; full UI pending CI |
| Unit/lint baseline green | 59 local tests pass; lint/format/type/full UI pending CI |
| Core domain needs no Textual imports | Import blocker and AST boundary tests pass |
| README/dev commands explicit and reproducible | README + DEVELOPMENT + CI + distribution probe; cross-platform execution pending CI |

## Review and handover

This report is implementer evidence, **not independent implementation review**.
Issue #10 and metaissue #1 remain open. No automatic closure or merge is requested.
A reviewer should inspect strict configuration failure behaviour, Windows path handling,
import side effects, packaging contents, actual Textual exit, test omissions and CI.
After independent Stone review and merge, reconcile OP-001 evidence before OP-002.
The existing EXECUTION_LEDGER explicitly requires merged OP-001 evidence for that frontier.

No live integration or Windows Terminal GUI session was exercised in the local container.
Headless UI checks are startup evidence, not durable job lifetime qualification.
Direct dependencies are pinned; transitive dependencies are recorded, not fully locked.
